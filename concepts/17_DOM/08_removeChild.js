// In order to remove a child element you need the parent element too.
// You have to select the child and removeChild.
// You remove an individual child or remove an entire div.

const pBlk = document.querySelector(".pBlk");
const cBlk = document.querySelector(".cBlk");

const heading = document.querySelector("#heading");

// pBlk.removeChild(cBlk);
// pBlk.removeChild(heading);
